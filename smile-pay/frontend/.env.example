# Get this from https://dashboard.stripe.com/test/apikeys (the PUBLISHABLE key, not secret)
# It MUST start with pk_test_ — this one is safe to expose in frontend code.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME

VITE_API_BASE_URL=http://localhost:4242
